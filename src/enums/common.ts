/**
 * 通用枚举配置
 *
 * @description 缓存 key、通用状态等定义
 */

/**
 * @description 本地缓存 key
 */
export enum EnumStorageKey {
	TOKEN = '__TOKEN__',
	REFRESH_TOKEN = '__REFRESH_TOKEN__',
	USER_INFO = '__USER_INFO__',
}
